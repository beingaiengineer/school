---
sidebar_position: 7
sidebar_label: "Callbacks & Hooks in Google ADK"
---

import LearningFlow from '@site/src/components/LearningFlow';

# Callbacks & Hooks in Google ADK

## 1. Quick Summary
| Area | Details |
|---|---|
| Topic | Callbacks & Hooks in Google ADK |
| Difficulty | Intermediate |
| Used For | Using hooks to intercept agent events in Google ADK. |
| Common Mistake | Blocking the main execution thread with synchronous hooks. |
| Performance | Depends on the framework and underlying LLM latency. |

## 2. Engineering Story

A healthcare company deployed a Google ADK-based agent to answer patient queries. Three weeks after launch, their compliance team raised a red flag: patients were occasionally including their Aadhaar numbers and phone numbers in free-text questions, and those strings were being logged verbatim in Cloud Logging — a HIPAA violation waiting to happen.

The engineering team's first instinct was to modify the agent's core logic to scrub PII before logging. But that would mean weaving PII detection into every tool and every response handler — messy, error-prone, and hard to audit. Instead, they registered a single `before_model_callback` and `after_agent_callback` in Google ADK. The before-hook redacted PII from the user's input before it ever touched the model. The after-hook scrubbed the response before it was logged. Zero changes to the agent's business logic. The callbacks acted like airport security — a checkpoint layer independent of what's on the plane. Compliance was achieved in one afternoon, not a sprint.

## 3. Real-World Analogy
Like setting an alarm clock. You tell the clock (the system) to wake you up (the callback) when a specific time happens (the event).

| Real World | Code Equivalent |
|---|---|
| Alarm Clock | Event Listener |
| Waking up | Callback Execution |
| Time condition | Event Trigger |

## 4. Concept Explanation
Callbacks and hooks in Google ADK allow you to inject custom logic at specific points in the agent lifecycle, such as before a tool is called, after a response is generated, or when an error occurs. This is critical for logging and observability.

## 5. Syntax Table
| Action | Code Example |
|---|---|
| Register Hook | `agent.on("before_tool", my_callback)` |
| Define Callback | `def my_callback(event): print(event)` |

## 6. Beginner Example
```python
def log_tool_usage(event):
    print(f"Tool {event.tool_name} was called.")

agent.on("before_tool_execution", log_tool_usage)
```

## 7. Real-World Engineering Example
```python
def async_audit_log(event):
    # Send to Google Cloud Logging non-blocking
    cloud_logger.log_async(event.data)

agent.register_hook("on_llm_response", async_audit_log)
```

## 8. Internal Working
Here is how it works under the hood. The flow represents the lifecycle of the agent's execution process.

<LearningFlow />

## 9. Performance Table
| Operation | Complexity |
|---|---|
| Initialization | O(1) |
| Tool Execution | O(N) where N is payload size |
| State Update | O(1) |

## 10. Top Interview Questions
| Question | Answer |
|---|---|
| Why use hooks instead of modifying the agent code? | Hooks allow separation of concerns, keeping agent logic clean while handling cross-cutting concerns like logging or metrics. |
| What is a common pitfall with callbacks? | Making them synchronous and blocking, which slows down the agent response time. |
| What are the security implications? | You must implement guardrails to prevent unauthorized tool execution or prompt injection. |
| When should you NOT use this? | When a simple direct LLM call or basic script would suffice. |
| How does it scale? | By distributing agent workloads across serverless functions or containerized environments. |

## 11. Tricky Questions & Edge Cases
Watch out for concurrency issues, bro. If multiple agents try to update the same shared state without proper locking mechanisms, you'll run into race conditions that are a nightmare to debug.

## 12. Real-World Usage
Used by top tech companies for internal tooling, customer support automation, and complex data pipeline orchestration. For instance, tech teams use these patterns to orchestrate delivery logistics dynamically based on real-time traffic data.

## 13. Best Practices
| DO | DON'T |
|---|---|
| Write comprehensive unit tests for individual agent tools | Skip error handling and assume the LLM will always return the perfect format |
| Use structured logging to trace agent decision paths | Hardcode sensitive API keys directly in the agent's configuration |
| Implement clear guardrails and timeout limits | Let an agent run indefinitely in an unbounded loop |

## 14. Production Notes
Warning: Always monitor memory usage and LLM token limits in production. A rogue agent stuck in a loop can quickly rack up massive API bills. Implement hard limits and alerting.

## 15. Common Mistakes
| Mistake | Fix |
|---|---|
| Ignoring logs | Add structured logging to track every step of the agent's thought process |
| Assuming perfect output | Use tools like Pydantic to strictly validate output schemas before proceeding |

## 16. Related Topics
- Framework Architecture Patterns
- Production Deployment Strategies
- Agent Observability

## 16. Top GitHub Repos
| Repository | Stars | Description | Why It Matters |
|---|---|---|---|
| [google/generative-ai-python](https://github.com/google/generative-ai-python) | ⭐ 4k+ | Official Google GenAI SDK | Foundation for Google ADK integrations |
| [langchain-ai/langgraph](https://github.com/langchain-ai/langgraph) | ⭐ 10k+ | Building stateful applications | Key competitor offering different paradigms |
| [joaomdmoura/crewai](https://github.com/joaomdmoura/crewai) | ⭐ 15k+ | Role-playing AI agents | Popular alternative for specific use cases |
